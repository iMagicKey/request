# UPDATE — imagic-request

> Audit performed: 2026-04-08. Version at time of audit: 1.1.0

---

## Backlog

- [ ] Support `AbortController` / `AbortSignal` for request cancellation in `stream()`
- [ ] Redirect following in `stream()`
- [ ] Retry support in `stream()`
